const greet = require("./server");

//! loading -> require is synchronous
//! loading -> import and export is asynchronous

// top level await

//! 1) the file which we are importing is executing first, after that the second file is getting executed
